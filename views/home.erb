<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Find a Game</title>
  </head>
  <body>
    <h1>Search Games</h1>
    <div class="searchbox">
      <h2>Search for a game by title</h2>
      <input type="text" name="title" value="Enter game title">
    </div>
    <div class="results">
      <h2>Search results</h2>
      <ul>
        <% [ 'Game 1', 'Game 2', 'Game 3' ].each do |game| %>
          <li class="game <%= game.downcase.gsub(' ','-') %>"><%= game %></li>
        <% end %>
      </ul>
    </div>
  </body>
</html>
